# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
# Local launcher output (Start-TennisAI.bat writes postgres/api/web logs here)
.local-logs

node_modules
dist
dist-ssr
*.local

# Local environment file — never commit (may hold secrets).
# .env.test and .env.example are intentionally tracked (flags only, no secrets).
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
